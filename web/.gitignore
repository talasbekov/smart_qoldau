node_modules/
.next/
next-env.d.ts

# Playwright
test-results/
playwright-report/

# Артефакт инкрементальной сборки TypeScript: меняется на каждом прогоне
# tsc и в истории только шумит.
tsconfig.tsbuildinfo
