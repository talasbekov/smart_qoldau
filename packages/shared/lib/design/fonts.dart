/// Забандленный шрифт интерфейса.
///
/// Раньше начертания брались через `google_fonts`, который тянет их по
/// HTTP на первом обращении. Для приложения, которым пользуются в кризисе,
/// это неприемлемо: без сети (или при недоступности fonts.google.com)
/// интерфейс уезжал на системный шрифт, а тесты печатали предупреждения о
/// неудачной загрузке. Теперь четыре начертания лежат в пакете.
library;

/// Имя семейства для [TextStyle.fontFamily].
///
/// Префикс `packages/shared/` обязателен: так Flutter ищет шрифт, объявленный
/// ВНУТРИ пакета. Без него движок искал бы семейство `Inter` среди шрифтов
/// приложения и не нашёл бы его.
const sqFontFamily = 'packages/shared/Inter';
