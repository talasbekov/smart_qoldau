-- Роль редактора библиотеки самопомощи (E13). Отдельным значением enum'а, а
-- не «пусть публикует суперадмин»: контентом занимается не тот же человек,
-- что верифицирует специалистов и видит деньги.
ALTER TYPE "AdminRole" ADD VALUE IF NOT EXISTS 'CONTENT_EDITOR';
