-- Удаление аккаунта по запросу (ТЗ §5.1). Строка пользователя не удаляется
-- физически: на неё ссылаются консультации, платежи и проводки — учёт
-- терять нельзя. Вместо этого PII вычищается, а строка помечается здесь.
ALTER TABLE users ADD COLUMN deleted_at TIMESTAMP(3);
