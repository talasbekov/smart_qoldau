// Р-20: порог качества эксперта — единый источник истины для двух
// потребителей (ревью задачи 9 E8a: раньше значения дублировались локально
// в обоих сервисах без проверки синхронности):
// - ReviewsService.checkRatingThreshold (E4, задача 7) — после каждого
//   пересчёта агрегатов эксперта пишет audit-флаг
//   expert.rating_below_threshold, если условие выполняется;
// - VerificationService.flaggedExperts (E8a, задача 9) — отдаёт ту же
//   очередь через GET /v1/admin/experts/flagged команде качества.
//
// Решение по упрощению из брифа задачи 7: audit-флаг фиксируется КАЖДЫЙ раз,
// когда условие выполняется после пересчёта — не только на пересечении
// порога вниз. Простая и предсказуемая реализация: очередь задачи 9 может
// дедуплицировать по времени, если понадобится не заваливать список
// повторными одинаковыми флагами.
export const RATING_THRESHOLD_COUNT = 20;
export const RATING_THRESHOLD_AVG = 4.0;
