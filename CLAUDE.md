# SmartQoldau — правила работы

## Дизайн следует прототипу

Весь интерфейс — мобильные приложения, веб-кабинет эксперта, веб-версия
клиента, лендинг и админка — делается **по прототипу v2** в хранилище
Obsidian: `/home/erda/Музыка/Obsidian_brain/smart_qoldau_vault/RAW/SmartQoldau (1)/`
(113 файлов, экспорт из дизайн-инструмента; словарь данных —
`docs/SmartQoldau-ER-Data-Dictionary.md` там же). Прежняя версия v1
(92 экрана) лежит в хранилище в `docs/Прототип/` и нужна только для
истории. Придумывать раскладку, когда для экрана есть прототип, нельзя:
сверяйся с ним до того, как писать виджеты.

Прототип — источник **раскладок**, а не продуктовых правил (ADR-006):
цены, проценты, таймеры, роли и статусы берутся из ТЗ и решений
Р-01…Р-27; всё, что прототип v2 вводит сверх этого, ждёт решений
владельца Q-01…Q-20 в `WIKI/planning/16_RISKS_QUESTIONS_AND_ADRS.md`.

Экраны веб-кабинета эксперта лежат там же:
`SmartQoldau Expert Web - Главная / Заявки / Консультации / Видеоконсультация /
Чаты / Клиенты / Расписание / Доход / Рейтинг / Уведомления / Профиль /
Поддержка`; экраны админки — `SmartQoldau Admin - …`. Соответствие экранов
прототипа маршрутам кода — `WIKI/planning/11a_SCREEN_REGISTRY.md`.

Если прототип расходится с реализованным поведением или с ТЗ — не
подгонять молча: назвать расхождение и решить явно (в
`WIKI/Расхождения в прототипе.md` хранилища уже есть разбор известных).

## Документация живёт в хранилище

В репозитории документации нет, кроме `docs/openapi.json` — это контракт
API, от него зависят `web api:generate` и контрактный тест
`packages/shared`. Всё остальное — в
`/home/erda/Музыка/Obsidian_brain/smart_qoldau_vault/`:

- `WIKI/planning/` — пакет аудита и мастер-плана переработки
  (`00_INDEX.md` — порядок чтения, `18_AI_EXECUTION_RULES.md` — правила
  для ИИ, `project-manifest.yaml` — машиночитаемый индекс). Читать перед
  любой задачей.
- `WIKI/` — ТЗ, PRD, бизнес-процессы, решения, планы эпиков.
- `docs/deeplinks.md` — схема ссылок приложения и сайта.

Инструментарий BMAD перенесён в `archive/bmad/` и не используется: вся
работа идёт через навыки superpowers.

## UI-задачи — через навык

Работу над интерфейсом веди через навык `ui-ux-pro-max`. Он применяется
там, где создаётся или меняется вёрстка и визуальная часть, а не там, где
правится бизнес-логика или API.

**Весь веб-фронтенд — только через `ui-ux-pro-max`, без исключений.**
Это относится ко всему, что рисуется и верстается в вебе: страницы и
компоненты `web/` (эпик E16, Next.js), админка, лендинг. Навык
вызывается ДО того, как написана первая строка разметки или стилей, —
и когда экран создаётся с нуля, и когда правится существующий.
Раскладка при этом всё равно берётся из прототипа: навык отвечает за
то, КАК сделано, прототип — за то, ЧТО на экране.

Исключение ровно одно: правка, не трогающая разметку и стили —
обработчик, запрос к API, состояние. Как только меняется то, что видно
глазом, — навык.

## Учёт работы — в трёх местах, обязательно

Каждая значимая порция работы отражается везде, а не в одном месте:

1. **Obsidian** (`/home/erda/Музыка/Obsidian_brain/smart_qoldau_vault/`) — планы эпиков и решения в `WIKI/`, ход работы
   в `LOG.md`, ночные и длинные сессии — отдельным файлом в `OUTPUT/`.
   Отдельный репозиторий со своим бэкапом, в основной не добавлять.
2. **Plane** — задачи и их состояния, проект «Smart Qoldau»
   (`~/plane/migration/plane_task.py`): новая работа заводится задачей,
   завершённая переводится в Done.
3. **Todoist** — проект «Smart Qoldau» зеркалит Plane один в один:
   те же номера SQD и названия, выполненные отмечены выполненными.

## Остальное
- TDD: тест → красный → минимальная реализация → зелёный → коммит; перед
  коммитом задачи — регресс затронутых пакетов.
- Код продукта меняется только после `PLAN APPROVED — START IMPLEMENTATION`
  и в рамках истории из `WIKI/planning/14_ROADMAP_AND_BACKLOG.md`.
